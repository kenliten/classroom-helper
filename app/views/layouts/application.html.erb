<!DOCTYPE html>
<html>
  <head>
    <title>Classroom Helper</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <%= yield %>
    <% if (notice.present?) %>
      <div class="fixed bottom-4 -translate-x-1/2 left-1/2 px-3 py-1 rounded shadow bg-green-200 text-green-500">
        <%= notice %>
      </div>
    <% elsif (alert.present?) %>
      <div class="fixed bottom-4 -translate-x-1/2 left-1/2 px-3 py-1 rounded shadow bg-yellow-200 text-yellow-500">
        <%= alert %>
      </div>
    <% end %>
  </body>
</html>
