<div class="w-[100vw] h-[100vh] flex flex-col items-center justify-center">
  <div>
    <img src="//picsum.photos/128" class="rounded-full block mx-auto">
    <h1 class="mb-4 text-4xl">Classroom Helper</h1>
  </div>
  <div class="flex">
    <%= link_to 'Registrarse', new_user_session_path, class:'border border-blue-500 text-blue-500 px-4 py-1' unless user_signed_in? %>
    <%= link_to 'Entrar', new_user_session_path, class:'border border-blue-500 bg-blue-500 text-white px-4 py-1' unless user_signed_in? %>
    <%= link_to 'Salir', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Deseas salir?' }, class:'border border-red-500 bg-red-500 text-white px-4 py-1' if user_signed_in? %>
  </div>
</div>